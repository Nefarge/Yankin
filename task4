#include <iostream>
#include <string>

using namespace std;

void f1(void);
double x, result;

void  main()
{
    cout.precision(4);
    cout << "x=" ;
    cin >> x;
    
    f1();
    cout << "f=" << result ;
}
void f1(void)
{
    result = (1 + sin(2 * x)) / cos(2 * x);
}
